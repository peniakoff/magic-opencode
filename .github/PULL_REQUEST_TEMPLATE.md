## Summary

<!-- Why this change exists. Link related issues. -->

## Packs touched

- [ ] OpenCode (`.opencode/` / `opencode.json`)
- [ ] Cursor (`.cursor/`)
- [ ] Docs / `.github` / meta only

If behavior changed, both packs are updated (or the exception is explained below).

## Dual-pack sync

<!-- Required when agents/commands/skills/scripts behavior changes. -->

## Test plan

- [ ] `bash -n` on touched scripts (or CI green)
- [ ] Manual smoke of `/work` or `/implement` path if orchestration changed
- [ ] Docs updated if install or safety model changed
